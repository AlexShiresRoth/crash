import React from 'react';
import Layout from '../layout/Layout';
import EpkComponent from '../epk/EpkComponent';

const Epk = (props: any) => {
	return (
		<Layout>
			<EpkComponent />
		</Layout>
	);
};

export default Epk;
